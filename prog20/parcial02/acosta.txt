# Prog. 1 - Parcial 2 - Elías Acosta

#include <stdlib.h>
#include <stdio.h>
#define T 11
int main(){
    int a[T];
    int i;
    for(i=0; i<T; i++){
        a[i]=i+1;
    }
    int n;
    int m;
    printf("Ingrese dos enteros\n");
    scanf("%d", &n);
    scanf("%d", &m);
    for(i=0; i<T; i++){
        if(i==0 || i % 2 == 0){
           a[i] = a[i] + n;
        }
        else{
           a[i] = a[i] + m;
        }
    }
    for(i=0; i<T; i++){
        printf("%d\n", a[i]);
    }
   int suma = 0;
   int mayores = 0;
   for(i=0; i<T; i++){
       if(a[i] < 20){
          suma = suma + a[i];
       }
       else if(a[i] >= 20){
             mayores = mayores + 1;
       }
   }
   printf("Suma de elementos menores a 20: %d\n", suma);
   printf("Cantidad de elementos mayores o iguales a 20: %d\n", mayores);
   
   char b[T];
  
  for(i=0; i<T; i++){
       if(a[i] >= 0 && a[i] <= 25){
          b[i] = a[i] + 'a';
                 
       } 
       else{
         b[i] = 'o';
          
       }                 
   }

  for(i=0; i<T; i++){
       if(a[i] >= 0 && a[i] <= 25){
          printf("%c. ", b[i]);
       }
       else{
          printf("%c", b[i]);
       }
      
   }
  printf("\n");
  int j = T;
  for(i=T; i>=0; i--){
      if (i != T){
        b[i] = b[j];
        printf("%c", b[j]);
      }else{
       printf("%c", b[i]);
      }
     j--;
  
  }        
  printf("\n");
 return 0;
} 

#include <stdio.h>

#define T 50
int mayor(int a, int b, int c){
    if(a > b && a > c){
       return a;
    }
    else if(b > a && b > c){
       return b;
    }
    else{
       return c;
    }
}
int f1(char d, int a){
       if(a >= 0){
          return d;
       }
       else{
          return d+1;
       }
}
int f2(int x, int y){
       printf("%d\n", x*y);
       return x;
}
void f3(int arr[]){
     int i;
     for(i=0; i<T; i++)
        if(i < T-1){
           printf("%d,", arr[i])
        }
        else{
           printf("%d.", arr[i])
        }
}
int sumar1(int a[], int b){
    int i;
    for(i=0; i<T; i++){
        a[i] = a[i] + b;
    }
    return b;
}
int sumar2(int b[]){
    int suma=0;
    int i;
    for(i=0; i<T; i++){
        suma = suma + a[i];
    }
    return suma;
} 
int main(){

  return 0;

}



