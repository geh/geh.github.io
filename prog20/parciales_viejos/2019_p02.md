% Parcial 2: arreglos y funciones

Mandar los archivos por mail a <guillaumh@gmail.com>, como archivos adjuntos.

Se deben entregar los dos archivos, sino no se regulariza el parcial.

# 1. Arreglos (3 puntos)

Hacer este ejercicio en un programa llamado `arreglo.c`.

En este ejercicio, definir una constante `T` con algun valor de tu elección.
Usala cada vez que sea relevante.

 1. Permití al usuario ingresar los valores de un arreglo de `T` elementos, e imprima:
    * La suma de todos los elementos del arreglo.
    * La suma de todos los elementos del arreglo que sean mayores (estrictamente) a 20.
    * Cuántos valores son mayores a 50.

 2. Luego, pedí la carga de dos arreglos de enteros de tamaño `T`  cada uno.
    Obtené la suma paralela de los dos arreglos, y guardá el resultado en un tercer arreglo del mismo tamaño.

 3. Modificá el programa para que, al final, imprime, línea por línea, todos los parámetros que recibió en línea de comando
    (no muestres su propio nombre). En el caso que no hay ningun parámetro en línea de comando, solo imprimir el nombre del programa.

# 2. Funciones (3 puntos)

Hacé este ejercicio en un archivo `funciones.c` que tenga la función `main` siguiente:

~~~C
#define T 50

main(){
}
~~~

Se trata de definir las funciones, no llamarlas.

  * El archivo tiene que ejecutarse sin error con `tcc -run funciones.c`.
  * La constante `T` sirve para especificar el tamaño de todos los arreglos. Usala cuando es necesario.
  * Si no está indicado que una función devuelve algun valor, es que no devuelve nada.
  * "Devolver" e "Imprimir" son dos cosas distintas.

 1. Definí una función `f1`, la cual recibe como parámetros tres enteros
    y devuelva el mayor de ellos.
 2. Definí una función `f2` que toma un caracter `d` y un entero `a`.
    Si `a` es mayor o igual a cero, la función devuelve `d`.
    Sino devuelve `d+1`.
 3. Definí una función `f3` que recibe dos parametros enteros
    `x` e `y` , imprime el valor de `x` multiplicado por `y`,
    luego devuelve `x`.
 4. Definí una función `f4` que toma un arreglo de enteros y
    que imprime todos sus elementos, separados por una coma,
    y al final terminado por un punto.
 5. Definí una función `f5` que toma como parametro un
    arreglo de enteros y un entero `b`. La función suma
    el entero `b` a cada uno de los elementos del arreglo,
    luego devuelve ese mismo entero `b` sin modificarlo.
 6. Implementá una función `int buscar(const int arr[], const int elem)`, que
    realiza la búsqueda de un elemento en un arreglo.
    Esta función devuelve:
    * el valor `-1` cuando el valor buscado no
      está en el arreglo.
    * sino, el primer valor `i` tal que `elem == arr[i]`
